`alpinejs-csp.min.js` was vendored from `@alpinejs/csp@3.15.11` so Firefox extension pages can run Alpine without `unsafe-eval`.

`lucide.min.js` was vendored from `lucide@0.547.0` so llastro previews and exported HTML can render Lucide icons without an extra network request.
